# Core OCR stack
paddleocr>=3.1,<4
paddlepaddle>=3.1,<4          # CPU build; swap for paddlepaddle-gpu (see README) on a CUDA host
# Required by PP-StructureV3; PaddleOCR's base install does not include it.
paddlex[ocr]>=3.1,<4
opencv-python-headless>=4.10,<5
PyMuPDF>=1.24,<2               # PDF text-layer + rasterization
numpy>=1.26,<3
Pillow>=10,<12                 # WEBP/TIFF/GIF decode fallback when OpenCV's codec can't

# Windows/oneDNN-safe fallback chain
rapidocr-onnxruntime>=1.3,<2
onnxruntime>=1.16,<2

# Office document formats (DOCX/PPTX/XLSX text layers)
python-docx>=1.1,<2
python-pptx>=0.6.23,<1
openpyxl>=3.1,<4

# Tests
pytest>=8,<9

# Optional / manual — see README "Vision fallback (Qwen-VL)":
# a Qwen-VL-capable client is NOT pinned here because it depends on how
# your Inference layer chooses to serve it (local vLLM/SGLang server,
# DashScope API, etc.). Install whichever client that server needs.
