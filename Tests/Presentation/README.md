# Presentation UI Preview (Vite)

يشغّل واجهة `Presentation/` (KutupAI Chat) محلياً بوضع Demo بدون Backend.

```bash
cd Tests/Presentation
npm install
npm run dev
```

يفتح المتصفح على `http://localhost:5173`
