# Orchestration Layer

Important: the connection between Orchestration and Agents is an in-process function call, not over the network - both are Python, preserving the 'Layered, not Microservices' principle.
